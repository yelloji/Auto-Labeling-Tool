Finalize UI locking, health-checker, migration & assorted backend/frontend updates

- backend/database/models.py
  * Added `process_pid = Column(Integer, nullable=True)` to the `TrainingSession` ORM model.
  * Updated comments to explain the new column.

- backend/main.py
  * Imported and started `start_training_health_checker` in FastAPI startup.

- backend/models/training/executor.py
  * Store spawned process PID in `TrainingSession` record.

- backend/models/training/training_extraction.py
  * Fix `data.yaml` path after ZIP extraction.

- backend/models/training/health_checker.py (new file)
  * Background service that polls DB for running sessions, checks PID with psutil, updates status.

- backend/scripts/add_process_pid.py (new file) – *excluded from this commit*.

- database.db
  * Fresh SQLite DB containing the new `process_pid` column.

- database_exports/database_export.json
  * Updated export reflecting current schema.

- Frontend component updates (all receive `disabled={isTraining}`):
  * ModelTrainingSection.jsx
  * IdentitySection.jsx
  * FrameworkTaskSection.jsx
  * PretrainedModelSelect.jsx
  * TrainingDatasetSection.jsx
  * PresetSection.jsx

- Updated implementation_plan.md, task.md, training_status_monitor_plan.md, training_runner_plan.md to reflect completed steps.
