# Development & Testing Dependencies
#
# SETUP — run BOTH commands on every new machine:
#   Step 1:  pip install -r test-suites/requirements-dev.txt
#   Step 2:  playwright install chromium
#
# Step 2 downloads the Chromium browser binary needed for UI tests.
# pip alone does NOT install it — both steps are always required.

# ── Backend API tests (pytest + FastAPI TestClient) ──────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0           # required by FastAPI TestClient async support
Pillow>=10.0.0          # used in conftest.py to create test images in-memory

# ── UI tests (Playwright browser automation) ─────────────────────────────────
playwright>=1.44.0
pytest-playwright>=0.5.0

# ── Code quality (optional but recommended) ──────────────────────────────────
# flake8>=7.0.0
# black>=24.0.0
