# Commit History (auto-logged by git hook)

## Session Notes (2026-04-02 to 2026-04-03)

- Active project: `stage-1-labeling-app - app-3-fix-release-system-422-error`
- Active branch: `feature/professional-logging-system`

- Guide-bot completed sections now considered stable enough:
- Upload
- Management
- Annotation Progress
- Manual Labeling
- Dataset
- Analytics
- Project Models
- Release

- Important global bot rules:
- UI -> bot sync must work
- bot -> UI sync must work
- explanation-first where users need understanding
- avoid duplicate explanations under different button names
- do not touch solved sections unless the user reports a real issue

## 2026-04-03 - Training Section Planning Started
- Training is now the next major bot section.
- No code changes yet for Training in this step.
- Current work is inspection and design only.

### Training section understanding so far
- Main file: `frontend/src/components/project-workspace/ModelTrainingSection/ModelTrainingSection.jsx`
- Training section contains:
  - Mode toggle: User / Developer
  - Identity
  - Framework & Task
  - Pretrained Model
  - Dataset release selection
  - Training Preset
  - right-side readiness/preflight tags
  - Start Training button
  - Config Preview tab
  - Status tab
- Important real subflows:
  - developer mode is password-protected
  - developer password can be changed
  - AI Console exists only in developer mode
  - AI Console also has its own terminal password prompt
  - GPU selection opens a small dialog for available GPU choice
  - if no training is running, Status can still show the last completed training result
- Dataset release behavior:
  - release dropdown uses releases from the same project
  - selected release zip is auto-prepared/unzipped for training
- User mode has fewer/simpler parameters.
- Developer mode exposes many more advanced parameters:
  - optimization
  - loss weights
  - augmentation
  - task/segmentation
  - validation

### Training bot design direction
- Training bot should feel like a strong AI guide but remain deterministic and lightweight.
- Use local UI state + rule-based knowledge, not live LLM/runtime internet dependence.
- Cover both User and Developer modes clearly.
- Give practical parameter-aware guidance, not generic descriptions.
- Prefer quality over quantity.
- User wants the bot to feel like a real expert person guiding someone through training safely and clearly.
- The bot should explain first, then offer real actions only where those actions truly reduce effort.
- Suggestions should feel intelligent, but must come from deterministic app knowledge, training rules, selected state, and parameter values.

### Expanded Training UI understanding
- User mode:
  - simpler training setup
  - readiness/preflight gate: `Name`, `Dataset`, `Model`
  - `Start Training` should not activate until those are ready
  - `Status` can show live training or the last completed training result when nothing is currently running
- Dataset selection:
  - release dropdown uses releases from the same project
  - selecting a release zip auto-prepares/unzips it for training
- Device flow:
  - switching from CPU to GPU opens an available-GPU selection dialog
- Developer mode:
  - protected by password dialog
  - has change-password flow
  - has AI Console button
  - AI Console has its own terminal password prompt
  - exposes many advanced parameter groups:
    - optimization
    - loss weights
    - augmentation
    - task & segmentation
    - validation

### Training design expectations
- Bot must cover:
  - user mode
  - developer mode
  - developer password modal
  - GPU selection dialog
  - AI Console
  - terminal password modal
  - live training state
  - last-result status view
  - parameter-aware guidance
- Bot should explain what is safe/simple in User mode versus advanced/risky in Developer mode.
- Bot should use real app constraints and selected values to give practical help.
- The design should support expert-like guidance without runtime LLM dependence.

### Collaboration plan for Training
- Training task is shared between:
  - user
  - me
  - Claude
- I should design the main Training bot architecture carefully first.
- Claude should inspect remaining Training subflows and challenge/improve the design, especially for missed UI states, synchronization risks, and better expert guidance opportunities.
- User will review the final Training design before coding starts.
- Final implementation should happen only after the design is considered clear enough by the user.

## Exact Next Step
- Training design is now being expanded from the real code/UI, still with no code changes.
- Detailed design reference file added:
  - `TRAINING_BOT_PLAN.md`
- Training design must cover:
  - user mode states
  - developer mode states
  - developer password modal
  - change-password modal
  - GPU selection dialog
  - AI Console
  - terminal password modal
  - live training state
  - initializing state
  - last-result status state
  - parameter-aware guidance strategy
  - central sync plan
  - collaboration split between me and Claude
  - the user's real expectation that this bot should feel like a real expert guide, not a generic form helper

## Expanded Training Detail (Design Phase)
- Main components inspected from real code:
  - `ModelTrainingSection.jsx`
  - `ModeToggle.jsx`
  - `TrainingDatasetSection.jsx`
  - `PresetSection.jsx`
  - `PretrainedModelSelect.jsx`
  - `IdentitySection.jsx`
  - `FrameworkTaskSection.jsx`
  - `TerminalPanel.jsx`
  - `LiveTrainingDashboard.jsx`
  - `TrainingInitializing.jsx`
- Real behavior confirmed:
  - training preflight depends on `Name`, `Dataset`, `Model`
  - dataset dropdown uses project releases filtered by task type
  - release zip selection can be auto-extracted/prepared
  - task/framework change can invalidate model selection
  - user mode has reduced settings but still meaningful controls
  - developer mode adds many advanced parameter groups
  - AI Console is draggable/resizable and separately password-protected
  - Status tab can show live training or the last completed result if nothing is running
  - developer mode and terminal access are separate protected subflows

## Collaboration Plan For Training
- This Training task is explicitly shared between:
  - user
  - me
  - Claude
- My role:
  - define the core Training bot architecture
  - define mode-aware states
  - define deterministic expert guidance rules
  - keep implementation centralized and safe
- Claude's role:
  - inspect remaining Training subflows and design gaps
  - suggest stronger state coverage and expert-style guidance improvements
  - challenge weak assumptions before coding starts
- User's role:
  - verify the bot feels like a real AI guide
  - review/approve the final design before implementation

## User's Real Intention For Training Bot
- The bot should feel like a real AI expert helping a human train a model.
- It should not feel like a generic tooltip form assistant.
- It should explain clearly, guide step by step, and provide useful actions when they reduce effort.
- It should give smart practical advice from real app state and rule-based knowledge.
- It should remain deterministic, lightweight, and suitable for the `.exe` app.

## Training Plan Revision Status
- `TRAINING_BOT_PLAN.md` has been rewritten again into a cleaner merged version.
- `TRAINING_BOT_PLAN.md` should now be treated as the single source of truth for Training bot design.
- Old appended review text or older draft structure should no longer be treated as separate active plans.
- Valid Claude review items already folded into the current plan:
  - dataset/model help moved out of fake snapshot states and into real help branches
  - `training-failed` included
  - strict snapshot priority order included
  - status states treated as mode-neutral design targets
  - dataset selected / extract / extracted subflow explained
  - explicit expert branches added for:
    - `Why is Single Class important here?`
    - `How does Smart Auto choose the optimizer?`
    - `Why is MuSGD recommended for YOLO26?`
    - grouped developer parameter explanations
  - `Start Training` now planned as a real bot -> UI action
  - detection/event work before coding is explicitly listed
- Current next expectation:
  - Claude should read the revised `TRAINING_BOT_PLAN.md` again and only point out real remaining gaps before implementation begins.
  - If a Claude suggestion is already merged into the plan, it should not be raised again as unresolved.

## 2026-04-03 - Training Bot Implementation Started
- Training bot implementation has now started from the finalized plan.
- New Training bot files added:
  - `frontend/src/components/guide-bot/pages/trainingBot.js`
  - `frontend/src/components/project-workspace/ModelTrainingSection/trainingGuideState.js`
- `GuideBot.jsx` now includes:
  - Training bot import
  - Training workspace refresh listener
  - Training direct-open detection
  - Training wizard routing
  - `training-*` close handling
- `guideScript.js` no longer uses the old static Training fallback in normal flow.
- Training UI now publishes centralized guide state from:
  - `ModelTrainingSection.jsx`
  - `ModeToggle/ModeToggle.jsx`
  - `Preset/PresetSection.jsx`
  - `Terminal/TerminalPanel.jsx`

### Training bot states now implemented
- `training-terminal-password`
- `training-dev-password`
- `training-dev-change-password`
- `training-gpu-dialog`
- `training-ai-console`
- `training-initializing`
- `training-running`
- `training-failed`
- `training-status-last-result`
- `training-user-empty`
- `training-user-ready`
- `training-developer-empty`
- `training-developer-ready`

### Training bot actions now implemented
- `Start Training`
- `Open AI Console`
- `Hide AI Console`

### Training implementation notes
- Single-class safeguard is respected in User mode before bot-triggered `Start Training`.
- Dataset release help is branch-based, not treated as a fake standalone page state.
- Last-result status is separated from live running and failed states.
- `TRAINING_BOT_PLAN.md` now includes implementation progress and testing focus, not only design.

## Exact Next Step
- Stay Training-only.
- Verify the implemented Training bot flow in real UI.
- Fix only real reported Training issues after user testing.
