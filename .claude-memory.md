# Commit History (auto-logged by git hook)

## Session Notes (2026-04-02 to 2026-04-03)

- Active project: `stage-1-labeling-app - app-3-fix-release-system-422-error`
- Active branch: `feature/professional-logging-system`

- Guide-bot completed sections now considered stable enough:
- Upload
- Management
- Annotation Progress
- Manual Labeling
- Dataset
- Analytics
- Project Models
- Release

- Important global bot rules:
- UI -> bot sync must work
- bot -> UI sync must work
- explanation-first where users need understanding
- avoid duplicate explanations under different button names
- do not touch solved sections unless the user reports a real issue

## 2026-04-03 - Training Section Planning Started
- Training is now the next major bot section.
- No code changes yet for Training in this step.
- Current work is inspection and design only.

### Training section understanding so far
- Main file: `frontend/src/components/project-workspace/ModelTrainingSection/ModelTrainingSection.jsx`
- Training section contains:
  - Mode toggle: User / Developer
  - Identity
  - Framework & Task
  - Pretrained Model
  - Dataset release selection
  - Training Preset
  - right-side readiness/preflight tags
  - Start Training button
  - Config Preview tab
  - Status tab
- Important real subflows:
  - developer mode is password-protected
  - developer password can be changed
  - AI Console exists only in developer mode
  - AI Console also has its own terminal password prompt
  - GPU selection opens a small dialog for available GPU choice
  - if no training is running, Status can still show the last completed training result
- Dataset release behavior:
  - release dropdown uses releases from the same project
  - selected release zip is auto-prepared/unzipped for training
- User mode has fewer/simpler parameters.
- Developer mode exposes many more advanced parameters:
  - optimization
  - loss weights
  - augmentation
  - task/segmentation
  - validation

### Training bot design direction
- Training bot should feel like a strong AI guide but remain deterministic and lightweight.
- Use local UI state + rule-based knowledge, not live LLM/runtime internet dependence.
- Cover both User and Developer modes clearly.
- Give practical parameter-aware guidance, not generic descriptions.
- Prefer quality over quantity.
- User wants the bot to feel like a real expert person guiding someone through training safely and clearly.
- The bot should explain first, then offer real actions only where those actions truly reduce effort.
- Suggestions should feel intelligent, but must come from deterministic app knowledge, training rules, selected state, and parameter values.

### Expanded Training UI understanding
- User mode:
  - simpler training setup
  - readiness/preflight gate: `Name`, `Dataset`, `Model`
  - `Start Training` should not activate until those are ready
  - `Status` can show live training or the last completed training result when nothing is currently running
- Dataset selection:
  - release dropdown uses releases from the same project
  - selecting a release zip auto-prepares/unzips it for training
- Device flow:
  - switching from CPU to GPU opens an available-GPU selection dialog
- Developer mode:
  - protected by password dialog
  - has change-password flow
  - has AI Console button
  - AI Console has its own terminal password prompt
  - exposes many advanced parameter groups:
    - optimization
    - loss weights
    - augmentation
    - task & segmentation
    - validation

### Training design expectations
- Bot must cover:
  - user mode
  - developer mode
  - developer password modal
  - GPU selection dialog
  - AI Console
  - terminal password modal
  - live training state
  - last-result status view
  - parameter-aware guidance
- Bot should explain what is safe/simple in User mode versus advanced/risky in Developer mode.
- Bot should use real app constraints and selected values to give practical help.
- The design should support expert-like guidance without runtime LLM dependence.

### Collaboration plan for Training
- Training task is shared between:
  - user
  - me
  - Claude
- I should design the main Training bot architecture carefully first.
- Claude should inspect remaining Training subflows and challenge/improve the design, especially for missed UI states, synchronization risks, and better expert guidance opportunities.
- User will review the final Training design before coding starts.
- Final implementation should happen only after the design is considered clear enough by the user.

## Exact Next Step
- Training design is now being expanded from the real code/UI, still with no code changes.
- Detailed design reference file added:
  - `TRAINING_BOT_PLAN.md`
- Training design must cover:
  - user mode states
  - developer mode states
  - developer password modal
  - change-password modal
  - GPU selection dialog
  - AI Console
  - terminal password modal
  - live training state
  - initializing state
  - last-result status state
  - parameter-aware guidance strategy
  - central sync plan
  - collaboration split between me and Claude
  - the user's real expectation that this bot should feel like a real expert guide, not a generic form helper

## Expanded Training Detail (Design Phase)
- Main components inspected from real code:
  - `ModelTrainingSection.jsx`
  - `ModeToggle.jsx`
  - `TrainingDatasetSection.jsx`
  - `PresetSection.jsx`
  - `PretrainedModelSelect.jsx`
  - `IdentitySection.jsx`
  - `FrameworkTaskSection.jsx`
  - `TerminalPanel.jsx`
  - `LiveTrainingDashboard.jsx`
  - `TrainingInitializing.jsx`
- Real behavior confirmed:
  - training preflight depends on `Name`, `Dataset`, `Model`
  - dataset dropdown uses project releases filtered by task type
  - release zip selection can be auto-extracted/prepared
  - task/framework change can invalidate model selection
  - user mode has reduced settings but still meaningful controls
  - developer mode adds many advanced parameter groups
  - AI Console is draggable/resizable and separately password-protected
  - Status tab can show live training or the last completed result if nothing is running
  - developer mode and terminal access are separate protected subflows

## Collaboration Plan For Training
- This Training task is explicitly shared between:
  - user
  - me
  - Claude
- My role:
  - define the core Training bot architecture
  - define mode-aware states
  - define deterministic expert guidance rules
  - keep implementation centralized and safe
- Claude's role:
  - inspect remaining Training subflows and design gaps
  - suggest stronger state coverage and expert-style guidance improvements
  - challenge weak assumptions before coding starts
- User's role:
  - verify the bot feels like a real AI guide
  - review/approve the final design before implementation

## User's Real Intention For Training Bot
- The bot should feel like a real AI expert helping a human train a model.
- It should not feel like a generic tooltip form assistant.
- It should explain clearly, guide step by step, and provide useful actions when they reduce effort.
- It should give smart practical advice from real app state and rule-based knowledge.
- It should remain deterministic, lightweight, and suitable for the `.exe` app.

## Training Plan Revision Status
- `TRAINING_BOT_PLAN.md` has been rewritten again into a cleaner merged version.
- `TRAINING_BOT_PLAN.md` should now be treated as the single source of truth for Training bot design.
- Old appended review text or older draft structure should no longer be treated as separate active plans.
- Valid Claude review items already folded into the current plan:
  - dataset/model help moved out of fake snapshot states and into real help branches
  - `training-failed` included
  - strict snapshot priority order included
  - status states treated as mode-neutral design targets
  - dataset selected / extract / extracted subflow explained
  - explicit expert branches added for:
    - `Why is Single Class important here?`
    - `How does Smart Auto choose the optimizer?`
    - `Why is MuSGD recommended for YOLO26?`
    - grouped developer parameter explanations
  - `Start Training` now planned as a real bot -> UI action
  - detection/event work before coding is explicitly listed
- Current next expectation:
  - Claude should read the revised `TRAINING_BOT_PLAN.md` again and only point out real remaining gaps before implementation begins.
  - If a Claude suggestion is already merged into the plan, it should not be raised again as unresolved.

## 2026-04-03 - Training Bot Implementation Started
- Training bot implementation has now started from the finalized plan.
- New Training bot files added:
  - `frontend/src/components/guide-bot/pages/trainingBot.js`
  - `frontend/src/components/project-workspace/ModelTrainingSection/trainingGuideState.js`
- `GuideBot.jsx` now includes:
  - Training bot import
  - Training workspace refresh listener
  - Training direct-open detection
  - Training wizard routing
  - `training-*` close handling
- `guideScript.js` no longer uses the old static Training fallback in normal flow.
- Training UI now publishes centralized guide state from:
  - `ModelTrainingSection.jsx`
  - `ModeToggle/ModeToggle.jsx`
  - `Preset/PresetSection.jsx`
  - `Terminal/TerminalPanel.jsx`

### Training bot states now implemented
- `training-terminal-password`
- `training-dev-password`
- `training-dev-change-password`
- `training-gpu-dialog`
- `training-ai-console`
- `training-initializing`
- `training-running`
- `training-failed`
- `training-status-last-result`
- `training-user-empty`
- `training-user-ready`
- `training-developer-empty`
- `training-developer-ready`

### Training bot actions now implemented
- `Start Training`
- `Open AI Console`
- `Hide AI Console`

### Training implementation notes
- Single-class safeguard is respected in User mode before bot-triggered `Start Training`.
- Dataset release help is branch-based, not treated as a fake standalone page state.
- Last-result status is separated from live running and failed states.
- `TRAINING_BOT_PLAN.md` now includes implementation progress and testing focus, not only design.

## Exact Next Step
- Stay Training-only.
- Verify the implemented Training bot flow in real UI.
- Fix only real reported Training issues after user testing.
- [2026-04-03 14:45] f91a92b feat(guide-bot): add Training section guide bot

## 2026-04-03 - Model Lab Mapping Started
- Claude created a full Model Lab UI map at:
  - `...memory/modellab_section_map.md`
- A project-root reference doc now exists:
  - `MODELLAB_BOT_PLAN.md`
- `MODELLAB_BOT_PLAN.md` currently contains only confirmed UI understanding so far.
- Confirmed so far:
  - 2-panel layout:
    - left `Trained Models`
    - right detail workspace
  - tabs:
    - Overview
    - Configuration
    - Model Manager
    - Validation
    - Comparison Engine
  - `Prediction` still pending full walkthrough
  - confusion matrix modal in Overview
  - Configuration split:
    - View Config
    - Advanced Config Editor
  - Model Manager:
    - best.pt
    - last.pt
    - args.yaml
    - download
    - add to project
  - Validation:
    - parameter form
    - validation history
    - results area
    - per-class breakdown
  - Comparison Engine:
    - baseline/challenger run selection
    - common image matching
    - metric winner summary
    - FP/FN/confidence delta cards
    - detailed comparison image viewer
- Next Model Lab step:
  - user will explain `Prediction` tab step by step
  - then `MODELLAB_BOT_PLAN.md` should be expanded into the full bot design

## 2026-04-04 - Model Lab Bot Implemented
- Model Lab now uses centralized guide state:
  - `window.__modellabGuideState`
  - `modellabGuideStateChanged`
- Added dedicated Model Lab bot logic:
  - `frontend/src/components/guide-bot/pages/modellabBot.js`
- Added Model Lab guide-state helper:
  - `frontend/src/components/project-workspace/ModelLabSection/modellabGuideState.js`
- Guide Bot now listens to Model Lab state and handles `modellab-*` wizard states.
- Model Lab bot now tracks and guides:
  - no trainings
  - empty selection
  - overview
  - config view / advanced
  - model manager
  - validation
  - prediction
  - prediction image viewer
  - prediction help
  - classify missing
  - prediction analytics overview / quality / charts / report
  - comparison engine
  - comparison viewer
  - confusion matrix modal
- Implemented Model Lab bot UI actions where the UI already supports them:
  - `Run Validation`
  - `Download Best Model`
  - `Download Last Model`
  - `Add Best Model to Project`
  - `Add Last Model to Project`
  - `Open Prediction Analytics`
  - `Send Settings to Training`
- Fixed real Model Lab bot bugs after review:
  - removed forced bot refresh on normal training-card selection
  - removed duplicated follow-up option after full-flow answer
  - added proper `modellab-` back/cancel close handling
- `Prediction Analytics -> Export` was removed from the live modal flow; `Report` remains the meaningful export/report ending.
- Build verification:
  - `npm run build` in `frontend/` passed
  - only existing repo warnings remained
- [2026-04-05 09:33] 5fc3304 fix(guide-bot): clear stale guide state on section switch and disable Create Release during generation
- [2026-04-05 10:47] f4ef979 feat(training): add Stop Training button and update bot guidance
- [2026-04-05 10:48] 7f38492 fix(release): correct class count to use full dataset summary instead of recent images sample
- [2026-04-05 10:58] 7d9841f fix(guide-bot): add stop training confirmation step before executing stop
- [2026-04-05 11:08] 2d712a2 fix(guide-bot): update training bot only when wizard type changes, not on every metric event
- [2026-04-05 11:10] a0c7b8c fix(guide-bot): replace undefined wizard() with explain() in stop confirmation
- [2026-04-05 11:42] 8472458 fix(release): auto-sync task type when YOLO format is selected and lock the field
- [2026-04-05 20:46] fd9244c fix(guide-bot): stop bot opening on validation name typing, add validation-running state
- [2026-04-05 21:09] a3c4169 fix(guide-bot): stop Model Lab bot auto-opening on every click
- [2026-04-05 21:14] 8832348 perf(validation): disable plots generation to speed up validation
- [2026-04-05 21:25] f3d4ee1 fix(exe): fix validation subprocess crash due to log write permission in Program Files
- [2026-04-05 21:34] d8dd919 fix: stop bot auto-opening on model-lab entry and reset orphaned running sessions on startup
- [2026-04-05 21:46] c602187 fix(guide-bot): reset refs immediately on section switch to prevent race condition
- [2026-04-05 21:49] 175d2f2 fix(validation): restore plots=True to fix confusion matrix always showing zero

## 2026-04-06 - Model Lab / Prediction Follow-Up Fixes
- Confirmed and fixed the real Prediction launch failure:
  - root cause was Windows command-length overflow (`WinError 206`) before `prediction_executor.py` started
  - fix: write prediction image list to `prediction_inputs.json` and pass a manifest path instead of huge `--images_json`
  - commit: `759370572765d0f1c147c9d073f73183e28c5ae5`
- Improved Prediction diagnostics:
  - `prediction.log` now captures real exceptions/tracebacks
  - invalid prediction logger categories were replaced with valid ones
- Fixed Prediction gallery overlay rendering for thumbnails:
  - changed gallery overlay aspect handling so boxes no longer stretch across the square card
  - commit: `b6b1821b593859a28c65fdfdefaad1cf1d5d5885`
- Found and fixed the real `missed` / `false positive` bug for fit-within prediction images:
  - old prediction experiments stored `input_images` as `image_name -> md5` only
  - GT matching expected width/height metadata and silently fell back to `640x640`
  - this broke segmentation GT polygon -> bbox conversion on non-square fit-within images
  - fix: save prediction `input_images` as `{ md5, width, height }`
  - commit: `4ca956a2e736b7e7150c8d0d3b0e3af290b0e21b`
  - user confirmed new prediction experiment results became correct again
- Simplified Release resize UI safely:
  - removed `Fill (with center crop)` and `Fit (reflect edges)` from exposed resize dropdown
  - hid `Preset Resolution` from release resize UI
  - kept backend compatibility for older saved configs
  - commit: `77630d8`
- Fixed remaining Model Lab bot reopen issue:
  - Model Lab `forceRefresh` events no longer reopen a bot that the user already closed
  - already-open bots still stay synced with Prediction / Analytics / Comparison / Validation modal state
  - commit: `b19ef3e`
- Tried the first Prediction gallery performance optimization:
  - added `thumbnail=true&size=...` support to the Prediction original-image endpoint
  - switched Prediction gallery cards to use thumbnail responses instead of full originals
  - kept Image Viewer on full original images
- Fixed a follow-up bug from that thumbnail optimization:
  - gallery overlay broke because thumbnail load dimensions overwrote original image dimensions
  - fix: keep using stored original `width` / `height` metadata for overlays when available, and only fall back to loaded image size for old experiments without metadata
- Remaining later-investigation item:
  - Prediction gallery, Prediction image viewer, and Release Detail View still feel slow on large image sets / large image sizes
  - likely performance issue, not a correctness issue
  - thumbnail optimization improved correctness safety but did not solve the speed issue enough
  - likely related to loading/decoding/rendering many large images plus overlays, and zip-backed image reads in Release Detail View

## 2026-04-07 - CEO/User Manual Direction
- Presentation feedback: CEO/non-technical reviewer needs a very simple user manual because the full UI is too much to understand quickly.
- New documentation direction:
  - create a beginner-friendly manual starting from Dashboard and moving downward through the workflow
  - use plain language, short steps, and screenshots later
  - avoid developer wording and avoid assuming the reader knows AI/model terminology
- Concepts the manual must clearly explain:
  - Dashboard: home/start screen for the app
  - Project: one workspace for a customer/task/use case, containing images, labels, releases, training, validation, prediction, and reports
  - Project Name: the human-readable name for that workspace
  - Model: the AI file that learns from labeled images and later predicts objects/defects
  - Global Model: base/pretrained model available across the app and used as a starting point
  - Custom Model: model trained from the user's own project data for a specific use case
  - Release: a prepared dataset version with images/labels used for training/validation
  - Training: teaching the model with a release
  - Validation: checking model performance on labeled data
  - Prediction: running the trained model on images to see detections
  - Report/Analytics: final summary for decision-making
- Manual should be built with screenshots from the app later; likely structure: What This App Does, Dashboard, Project, Models, Upload/Label, Release, Training, Validation, Prediction, Report.

## 2026-04-07 - User Manual / Global AI Models Notes
- New priority: create a simple Word-ready user manual for a non-technical/CEO audience.
- Manual style:
  - simple English
  - one UI screenshot per section
  - explain what the user is seeing, what each important field means, and what to do next
  - avoid mixing concepts and avoid technical wording where possible
- Dashboard manual wording should say:
  - this is the first page of the app
  - user can quickly check how many projects exist
  - user can see total images across projects
  - user can see labeling progress and how many images are still unlabeled
  - user can open recent projects from the recent project list
- Global AI Models page was renamed in UI from `AI Models` to `Global AI Models` (commit `5e21010`).
- Global AI Models manual wording decision:
  - do not mix Pre-trained YOLO Models and Global Custom Models
  - Pre-trained YOLO Models are built-in/general starting models available in the app
  - Global Custom Models are company-specific models added to the Global AI Models page
  - a Global Custom Model may be uploaded manually or created from a model trained in Gevis AI Studio
  - good Global Custom Models can help auto-label similar images and can be used as a better starting point for new training in similar projects
- Model type wording:
  - Object Detection: model finds objects and draws a rectangle/box around each object
  - Segmentation: model finds the detailed shape of the object and draws a contour around it; useful when the exact outline is important
- Upload Global Custom Model manual wording:
  - explain Model Name, Description, Model Type, Model File, Training Input Width, and Training Input Height
  - Training Input Width/Height should match the image size used when that model was trained, e.g. 1568 x 1568
  - after upload, the model appears in Global AI Models with a Custom tag and can be used when Ready
- Provided screenshots for this manual section:
  - `globel-model.png`: place first for Global AI Models page explanation
  - `upload.png`: place second for Upload Global Custom Model explanation

## 2026-04-08 to 2026-04-09 - User Manual Workflow Progress
- Manual is being written mainly in Word, but draft structure/content is mirrored in:
  - [`docs/USER_MANUAL_DRAFT.md`](v:\stage-1-labeling-app\app-3-fix-release-system-422-error\docs\USER_MANUAL_DRAFT.md)
  - [`docs/USER_MANUAL_HANDOFF.md`](v:\stage-1-labeling-app\app-3-fix-release-system-422-error\docs\USER_MANUAL_HANDOFF.md)
- User prefers:
  - exact screenshot placement notes inline where each screenshot belongs
  - simple, story-like instructions for non-technical users
  - no over-technical explanation unless needed
  - actual UI flow order, not abstract explanation order
- Word/doc structure guidance decided:
  - `Heading 1` = chapter
  - `Heading 2` = main workflow step
  - `Heading 3` = subsection
  - user uses Word section breaks / headers / step badges manually

### Workflow / Chapter 3 decisions
- Flow chart remains high-level:
  - Projects -> Upload -> Manual Labeling -> Dataset Split -> Dataset Verification -> Release -> Model Training -> Model Lab -> Validation -> Prediction -> Analytics / Report
- Actual UI screens are explained inside sections even if flow chart names are simpler.

### Step 1 - Projects
- Use real project example:
  - `Sensore di Forza Internal`
- `Project` should be explained simply as one work folder in the app containing images, labels, training, validation, prediction, and reports.

### Step 2 - Upload
- Main upload methods emphasized:
  - `Select File(s)`
  - `Select Folder`
  - `Select Folder (images + labels)`
  - `Upload Videos and Extract Frames`
- Important notes:
  - Batch name prompt is mainly relevant when manually selecting files
  - selecting a folder can use the folder name directly
  - `images + labels` is for importing already-labeled YOLO data:
    - images
    - `.txt` annotations
    - `data.yaml`
  - video upload asks for FPS and output image format before extracting frames
- API / cloud upload options were judged not working and should not be described as active workflow options.

### Step 3 - Manual Labeling
- Very important corrected flow:
  - Upload -> Unassigned -> Annotating -> Open batch -> Open annotation area -> Choose Manual Labeling -> Annotation Progress -> Start Labeling
- Auto Labeling is future version; current workflow is Manual Labeling.
- Screenshot order for Step 3 must stay inline in this exact sequence:
  - `unassigned.png`
  - `annotaating.png`
  - `annotation-progess.png`
  - `enterinto mnaullabeing ui.png`
  - `after-drawing anntion. crets label first time.png`
  - `labeled-imaeg in-manula-lableinui.png`
- Tool wording decisions:
  - For `Object Detection Task`, user must use `Box`
  - For `Segmentation Task`, user must use `Polygon`
  - `Smart Polygon` can also be used for faster contour drawing
  - `Null` is for images with no object to label / no normal annotation
  - `Select` is for clicking, reviewing, or editing an existing annotation
- First label example changed:
  - use `scratch` as first class label example
- Multiple class label explanation added:
  - project can contain multiple class labels like `CUT`, `HOLE`, `scratch`, `defects`
  - if a label already exists, user can select it from available labels instead of creating it again
- End of Step 3 wording:
  - after labeling all images in the batch, manual labeling work for that batch is completed
  - next step is Dataset Split
  - user goes back from manual labeling screen to annotation progress page

### Step 4 - Dataset Split
- Structure chosen:
  - annotation progress complete screen
  - click `Add Images to Dataset`
  - popup with split method dropdown + selected split method / slider side by side
- Dataset Split explanation should stay simple:
  - divide labeled images into Train / Validation / Test for AI training
- Side-by-side popup explanation:
  - left image = all available split options
  - right image = selected split method and percentages
- Main split method emphasized:
  - `Split Images Between Train / Valid / Test`
  - slider decides percentages / counts
- Other options explained briefly:
  - `Use Existing Split`
  - `Add All Images to Train Set`
  - `Add All Images to Valid Set`
  - `Add All Images to Test Set`
- Closing text:
  - after applying dataset split, labeled batch images move into Dataset column
  - user then continues to Dataset section to verify split assignment

### Step 5 - Dataset Verification
- Dataset Verification is done in actual `Dataset` screen/page.
- Purpose:
  - verify dataset images after dataset split
  - especially useful if one batch or multiple batches were added to Dataset column
- What user checks:
  - dataset images are visible
  - images are in correct split
  - correct batch images are available in dataset
- Useful filters to mention:
  - split
  - dataset
  - class

### Step 6 - Release
- Release section intro should explain outcome in simple way:
  - user prepares final dataset package for AI model training
  - sets release options
  - adds transformations if needed
  - chooses export format
  - creates release
  - final output is ZIP file for AI model training
- Release UI explanation structure so far:
  - main release page
  - rebalance split
  - add basic transformation
  - resize transformation
  - added transformations
  - release configuration
  - download release
  - release created / release history
  - release detail view
- Empty Release History screenshot is acceptable and simpler for manual.
- Rebalance should be mentioned briefly in main UI because visible; no deep explanation required.
- Rebalance wording simplified to:
  - adjust Train / Validation / Test image counts before release creation
  - useful if user wants to change split distribution
- Basic transformations:
  - list briefly only; no deep explanation of every basic tool
- Resize explanation:
  - use short when-to-use notes for `Stretch to`, `Fit within`, `Fit (black edges)`, `Fit (white edges)`
- Advanced transformations:
  - do not create heavy extra explanation; only short note that they can also be added in same way
- Release Configuration decisions:
  - task type is locked/handled automatically for YOLO path
  - user mainly chooses export format
  - if labeled with `Polygon`, choose `YOLO Segmentation`
  - if labeled with `Bounding Box`, choose matching `YOLO Object Detection` export format
  - `Preview Output` explanation should focus on final release summary before create
- Download Release popup notes:
  - after release is created, Download Release window opens automatically
  - only mention working choices:
    - download ZIP
    - close window
  - if user clicks Close, app returns to Release page
- Release page after creation:
  - release appears in Release History
  - from there user can review / download again
- Release Detail View:
  - explain only as screen to review created release, release images, and annotations
  - mention user can click any image for detailed view; no need to explain image viewer yet

### Label bugs fixed during manual work
- Real manual-label case-variant bug fixed and committed:
  - typing `SCRATCH` when `scratch` already exists no longer creates separate class
  - frontend now normalizes to canonical existing label name
  - backend label sync also dedupes case-insensitively
  - commit: `8f97ff2` `fix(labels): normalize manual labeling case variants`
- Earlier stale-label leakage fix remains important:
  - commit: `cf998d5`

### Next planned section
- Next section to document is `Model Training`.
- User wants to document only **User mode**, not Developer mode.
- Current available screenshot:
  - `MODEL-TRAING-EMPTY.png`
- Likely best training section structure:
  - simple intro: what user does / what user gets
  - empty training screen
  - filled/running training screen (still needed later)
- Model selection guidance decided for non-technical users:
  - first choose correct task type
  - then only matching pretrained models appear
  - app has YOLO11 and YOLO26, each with Nano and Small, for Object Detection and Segmentation
  - start with Nano
  - if result not good enough, try Small
  - compare YOLO11 and YOLO26 to find best model for the project

### Transformation fixes - 2026-04-13
- Commit completed:
  - `9a81101 fix(transformations): split flip variants and align rotation annotations`
- Flip behavior changed:
  - `Flip` remains one UI tool
  - `Horizontal` creates one horizontal flip output
  - `Vertical` creates one vertical flip output
  - `Both` creates one combined horizontal + vertical flip output
  - if Horizontal and Vertical are both enabled, they create separate output images
  - if Both is enabled, it creates the combined output image
- Flip preview changed:
  - single selected flip option shows normal preview
  - multiple selected flip options show preview tabs
  - preview tabs do not change saved release config
- Backend + debug transformer files were updated for flip variants and `flip_both` naming.
- Plain Rotate annotation math was adjusted to match the screen-coordinate / PIL-style rotation direction in live and debug annotation transformer files.
- Current pending investigation:
  - rotation image output looks correct, but generated annotation transform can still be wrong when using rotation alone or rotation combined with other tools
  - next step is to read the release generation + annotation transform path and identify the real root cause
  - do not change rotation code until user gives explicit permission
- `ReleaseDetailsView copy.jsx` was confirmed as an unused tracked duplicate and is not imported by the app. User deleted it manually; deletion is currently pending/uncommitted unless committed later as a separate chore.

### Rotation bbox investigation - 2026-04-13
- After the rotate canvas fix, polygon/segmentation rotation looked correct in UI.
- Commit made for that fix:
  - `689dbdf fix(transformations): align rotated polygon annotations with expanded canvas`
- Remaining issue:
  - bounding box rotation still looks too large/loose in release detail view for object detection examples.
- Temporary debug prints were added in original file only:
  - `backend/core/annotation_transformer.py`
  - `_transform_bbox`
  - marker text:
    - `=== BBOX ROTATION DEBUG: ROTATE STEP ===`
    - `=== BBOX ROTATION DEBUG: AFTER RESIZE ===`
  - These prints must be removed after investigation.
- Debug output was saved in:
  - `debug.md`
- Important debug numbers for `car.jpg` / dog bbox:
  - original image: `800 x 600`
  - rotation: `39.8`
  - PIL expanded canvas: `1000 x 974`
  - original bbox example 1: `(135.938, 120.312, 584.375, 598.438)`
  - bbox after rotate before resize: `(182.105, 230.929, 832.685, 885.314)`
  - after mandatory resize `stretch_to 800 x 450`: `(145.684, 106.692, 666.148, 409.026)`
  - original bbox example 2: `(190.625, 43.750, 701.562, 598.438)`
  - after mandatory resize `stretch_to 800 x 450`: `(140.090, 44.859, 738.175, 392.853)`
- Current reasoning:
  - bbox transform is rotating the original axis-aligned rectangle's 4 corners, translating into PIL expanded canvas, then applying mandatory resize.
  - This is mathematically expected for normal YOLO detection boxes, but can be visually loose after large rotations because normal YOLO bbox cannot represent a tilted rectangle or object shape.
  - User believes there may still be a transform bug because manually drawn bbox on the rotated object can be much tighter.
  - Need sanity-check with Claude/another reviewer before deciding final solution.
- Possible product options to discuss:
  - keep normal bbox math and recommend small rotation angles for bbox-only object detection
  - if segmentation polygon exists, optionally derive detection bbox from transformed polygon for tighter boxes
  - do not use YOLO OBB inside normal YOLO detection export because it is a different task/export format
  - adding true OBB support would be a separate bigger feature

### Current transformation status - 2026-04-14
- Transformation work reached a stable/safe state for now.
- Recent commits:
  - `975b666 Fix rotation preview and release behavior`
  - `f5ac53a Hide affine transform from release UI`
  - `7f72455 Hide perspective warp from release UI`
- Rotate behavior:
  - UI preview has `Object Detection` and `Segmentation` preview modes.
  - Object Detection rotate preview/release uses same-frame behavior to match normal YOLO bbox expectations better.
  - Segmentation rotate preview/release keeps full expanded rotated image so polygons/contours remain intact.
  - Rotate angle range is now limited to `-45` to `+45`.
  - UI warns when angle is greater than about 15 degrees because large rotations can be risky for object detection boxes.
- Hidden risky tools:
  - `affine_transform` hidden from advanced transformation UI and pending list.
  - `perspective_warp` hidden from advanced transformation UI and pending list.
  - Backend for those tools was not deleted; only UI exposure was hidden because precise annotation transformation is not trusted enough for release/training.
- Current visible/usable release tools:
  - Flip
  - Rotate
  - Resize
  - Random Zoom
  - Crop and other basic safe transforms as already present
- Do not re-enable `affine_transform` or `perspective_warp` unless backend annotation transformation is redesigned and tested.

### Full Project Export / Import research - 2026-04-14
- User wants a professional **full project backup/restore** feature, not only COCO export.
- Priority decision:
  - Project Export / Import is more important than COCO export right now.
  - COCO can be a later release-format task after the project-transfer feature.
- A reference document was created:
  - `docs/PROJECT_FULL_EXPORT_IMPORT_REFERENCE.md`
- Reference project used for mapping:
  - `Sensore di Forza internal`
  - project DB id: `8`
  - project type: `segmentation`
- Reference project DB snapshot found:
  - 3 datasets
  - 22 images
  - 88 annotations
  - 5 labels: `defects`, `null`, `scratch`, `CUT`, `HOLE`
  - 4 releases
  - 9 image transformation rows
  - 1 training session: `SENSORE-FIRST`
  - 3 model experiments: 1 validation, 2 prediction
  - 0 human_verifications for this specific project
- Important: `human_verifications` table is still mandatory for full project transfer because other projects use it for prediction review pass/fail/missing/manual records.
  - Current DB has 142 `human_verifications` rows under project `gevis`.
  - This table stores status like `pass`, `fail`, `missing`, plus bbox, class, image name/hash, manual flag, and experiment id.
- Reference project folder summary:
  - `annotating`: 0 files
  - `dataset`: 4 files, about 9.18 MB
  - `model`: 242 files, about 51.96 MB
  - `releases`: 4 files, about 918.76 MB
  - `training_data`: 1763 files, about 925.34 MB
  - `unassigned`: 18 files, about 41.31 MB
- Key design agreement:
  - User should export/import a single ZIP.
  - User should not manually extract ZIP.
  - App should internally extract, validate, copy files, insert DB rows, and refresh Projects page.
  - Full restore is preferred over compact export.
  - Include the complete selected project folder, including `training_data/`, even if the ZIP becomes large.
  - Do not export global app model folders like root `models/`, `yolo/`, or `yolo26seg/`; those belong to the app/EXE install.
  - Export only project-owned/custom/trained model files/DB rows.
- Recommended package shape:
  - `manifest.json`
  - `database_snapshot.json`
  - `files/<full project folder>/...`
- App paths are mostly relative, e.g. `projects/Sensore di Forza internal/...`.
  - If imported project name stays the same and no name conflict exists, path rewriting may be minimal.
  - If target app already has a project with the same name, app must ask user for a new imported project name.
  - If project name changes, imported DB relative paths containing the old project folder name must be rewritten to the new folder name.
- Main hard part:
  - not file copy
  - DB row extraction and safe ID remapping
- Required ID maps:
  - old project id -> new project id
  - old dataset id -> new dataset id
  - old image id -> new image id
  - old label id -> new label id
  - old ai model id -> new ai model id
  - old release id -> new release id
  - old training id -> new training id
  - old experiment id -> new experiment id
- Important remap corrections:
  - `annotations.class_id` must use label id map.
  - `annotations.model_id` must use AI model id map when model is project/custom/trained.
  - `auto_label_jobs.model_id` must use AI model id map when model is project/custom/trained.
  - `training_sessions.base_model_id` must either map to an imported project/custom/trained model or resolve to a global/default model installed on target app.
  - `releases.datasets_used` and `releases.config` can contain dataset IDs, not only paths; parse JSON and remap IDs.
  - `model_experiments.predictions` and `input_images` can contain paths; parse and rewrite if project folder name changed.
- Import safety:
  - use staging folder for ZIP extraction
  - use DB transaction for insert/remap phase
  - rollback DB on failure
  - delete copied project folder if import fails after file copy
  - only show success after integrity checks pass
- Tables to include in full export/import design:
  - `projects`
  - `datasets`
  - `images`
  - `annotations`
  - `labels`
  - `dataset_splits`
  - `releases`
  - `image_transformations`
  - `training_sessions`
  - `model_experiments`
  - `human_verifications`
  - `label_analytics`
  - `auto_label_jobs`
  - `image_variants`
  - `ai_models`
- Recommended backend implementation location:
  - do not add this into huge `releases.py`
  - create dedicated transfer files:
    - `backend/api/routes/project_transfer.py`
    - `backend/api/services/project_export_service.py`
    - `backend/api/services/project_import_service.py`
    - `backend/api/services/project_transfer_manifest.py`
- Suggested endpoints:
  - `POST /api/v1/projects/{project_id}/export`
  - `POST /api/v1/projects/import/validate`
  - `POST /api/v1/projects/import`
- Suggested frontend placement:
  - Projects page
  - per-project `Export Project`
  - page/top-level `Import Project`
- Practical next step:
  1. Read `docs/PROJECT_FULL_EXPORT_IMPORT_REFERENCE.md`.
  2. Confirm exact DB export/import table order.
  3. Implement export service first: produce ZIP with `manifest.json`, `database_snapshot.json`, and full project folder.
  4. Implement import validate next: inspect ZIP, detect project-name conflict, report required new name.
  5. Implement import write path last: copy folder, insert rows, remap IDs, rewrite relative paths only if project folder name changed, then run integrity checks.
- [2026-04-15 14:39] 2e9c4b8 add application logo assets
- [2026-04-17 16:12] cebcb96 feat(guide-bot): add Export and Import guidance to Projects page bot
- [2026-04-17 16:13] 090ec90 chore: remove stale docs and old backup files, update memory and db
- [2026-04-21 10:20] 3a25a64 feat(release): add Tile transformation for large image splitting
- [2026-04-21 10:23] f35ede0 fix(release): add tile to basic transformation categories list
- [2026-04-21 10:24] ccf5ce3 fix(release): tile preview shows full image with grid overlay instead of single cropped tile
- [2026-04-21 10:34] 124890e fix(release): tile preview tabs - Grid Layout and Sample Tile, revert original image changes
- [2026-04-21 10:45] 8193f68 fix(release): treat tile as base transformer - exclude from augmentation schema, update real image count after ZIP
- [2026-04-21 10:48] 88cd1a6 fix(release): exclude tile from max images per original calculation

## 2026-04-21 - Tile Transformation Feature Completed

### What was built
- New `Tile` transformation tool in Release Transformations (Basic category)
- Splits large images (e.g. 6000×5000) into an N×M grid of tiles for YOLO training
- Each tile gets remaining transforms + resize applied last (mandatory-resize-last rule unchanged)
- Annotations remapped per tile via existing crop annotation pipeline

### Key rules established
- Tile is a **base transformer** like Resize — not an augmentation/variation tool
- One image → exactly cols×rows tiles, always deterministic
- `Images per Original` multiplier does NOT apply when Tile is used
- Release history shows actual output tile count (DB updated after ZIP creation)

### Preview behavior in TransformationModal
- "Grid Layout" tab: full image with white grid lines drawn on it
- "Sample Tile" tab: one randomly chosen tile
- Original panel: always unchanged raw image (never modified by tile or any other tool)
- Changing cols/rows sliders updates both preview tabs live

### Files changed
- `backend/api/services/image_transformer.py` — `_apply_tile()`, registered in transformation_methods, `get_available_transformations()`
- `backend/api/routes/releases.py` — tile early-exit loop in main image processing; DB count update after ZIP
- `backend/api/routes/transformation_preview.py` — tile in `categories.basic`; injects `_preview_mode` into tile config
- `backend/core/transformation_schema.py` — tile skipped in combination generation (both paths, same as resize)
- `backend/core/transformation_config.py` — tile excluded from `calculate_max_images_per_original`
- `frontend/.../TransformationSection.jsx` — tile icon ⊞
- `frontend/.../TransformationModal.jsx` — tile icon, two preview tabs, tab handler, preview_mode routing

## Exact Next Step
- User will describe new features to develop. Waiting.
- [2026-04-21 11:01] d8cd04b chore: update memory with Tile transformation feature notes
